{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.product_edit'|trans({'%id%': product.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.product_edit'|trans({'%id%': product.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'PUT', action: url('edit', {id: product.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('homepage') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}